const initialState = {
  selectedContact: {},
  allContact: [],
};

const addressbook = (state = initialState, action) => {
  switch (action.type) {
    case '':
    default:
      return state;
  }
};

export default addressbook;
